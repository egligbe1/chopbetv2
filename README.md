# ChopBet - Football Prediction Platform

ChopBet is a professional-grade football prediction web application that uses Google Gemini API with Search Grounding to research and predict the best matches of the day.

## 🏗️ Architecture

- **Backend**: Python with FastAPI
- **Database**: PostgreSQL with SQLAlchemy ORM
- **Frontend**: Next.js 14 (App Router) with Tailwind CSS
- **AI Engine**: Gemini 1.5 Pro with Google Search Grounding

## 🚀 Getting Started

### Prerequisites

- Python 3.10+
- Node.js 18+
- PostgreSQL

### Backend Setup

1. Navigate to the `backend` directory:
   ```bash
   cd backend
   ```
2. Create a virtual environment and activate it:
   ```bash
   python -m venv venv
   source venv/bin/activate  # On Windows: venv\Scripts\activate
   ```
3. Install dependencies:
   ```bash
   pip install -r requirements.txt
   ```
4. Configure environment variables in `.env` (copy from `.env.example`).
5. Run database migrations:
   ```bash
   alembic upgrade head
   ```
6. Start the FastAPI server:
   ```bash
   uvicorn main:app --reload
   ```

### Frontend Setup

1. Navigate to the `frontend` directory:
   ```bash
   cd frontend
   ```
2. Install dependencies:
   ```bash
   npm install
   ```
3. Configure environment variables in `.env.local` (copy from `.env.local.example`).
4. Start the development server:
   ```bash
   npm run dev
   ```

## 🛠️ Features

- **Automated Predictions**: Runs every morning at 7:00 AM UTC.
- **Results Checker**: Runs every night at 11:00 PM UTC to verify outcomes.
- **Accuracy Tracking**: Historical performance stats across leagues and markets.
- **Admin Controls**: Trigger prediction runs and results checks manually.

## ⚖️ Disclaimer

Predictions are generated by AI for informational purposes only. No financial advice provided. Use at your own risk.
